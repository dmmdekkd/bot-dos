{
    "builds": [
      {
        "src": "/package.json",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "src/.vuepress/dist",
          "buildCommand": "pnpm run docs:build"
        }
      }
    ]
  }
  